---
interface Props { text: string }
const { text } = Astro.props;
---

<span class="inline-flex items-center border px-2 py-1 text-[0.68rem] uppercase tracking-[0.08em] text-muted hairline">{text}</span>
